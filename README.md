# Mineral_processing

Step 1.... Create a virtual environment and activate it.

Step2.... (Optional) Run analysis.ipynb to generate the required files.
        Note: analysis file is already present but incase if not available then you will have to generate it and once you run the code wait for atleast 8-10 hours to generate the files.

Step3.... use mineral_scripts.ipynb file for data analysis and ML model

Use dashboard.py file to run the streamlit app

use command streamlit run dashboard.py to run the app

note: in case path error is found then use streamlit run "path to dashboard.py"




